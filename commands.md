```
python - -version
pip - -version
python - m venv .venv
source .venv/bin/activate
pip install "fastapi[standard]"
fastapi dev main.py
pip install sqlalchemy
docker compose up -d
docker compose down
```
